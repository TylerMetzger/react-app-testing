{
  "name": "react-app-testing",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"nodemon backend/server.js\" \"cd frontend && yarn start\"",
    "install-all": "concurrently \"yarn install\" \"cd frontend && yarn install\"",
    "build": "cd frontend && npm run build",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.7",
    "react-router-dom": "^5.2.0"
  }
}
